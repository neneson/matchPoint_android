<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Sólo relojes: Play no la ofrecerá a teléfonos. -->
    <uses-feature
        android:name="android.hardware.type.watch"
        android:required="true" />

    <!-- Fase 4: ambient mode (always-on) durante el partido. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Fase 4: Ongoing Activity = notificación + servicio en primer plano. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Standalone: funciona sin app de teléfono (v1 sin login, ver Fase 5). -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:taskAffinity=""
            android:theme="@style/MatchpointTheme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Mantiene vivo el partido y lo publica en el watch face. `specialUse` porque
             no encaja en ningún otro tipo: no es ejercicio medido ni sincronización de
             datos, es un marcador que el usuario deja corriendo. -->
        <service
            android:name=".service.MatchService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Marcador de tenis visible mientras dura el partido" />
        </service>
    </application>

</manifest>
