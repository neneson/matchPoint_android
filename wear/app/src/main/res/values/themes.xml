<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Pantalla de arranque: sólo el fondo, para que no aparezca un flash blanco
         antes de que Compose dibuje. -->
    <style name="MatchpointTheme.Splash" parent="@android:style/Theme.DeviceDefault">
        <item name="android:windowBackground">@color/background</item>
    </style>
</resources>
