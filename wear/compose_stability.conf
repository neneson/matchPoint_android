// Clases que el compilador de Compose debe tratar como estables.
// Ver el bloque `composeCompiler` de app/build.gradle.kts.
//
// El dominio es Kotlin puro e inmutable (`data class` con `val` y `equals` por valor);
// el compilador no puede deducirlo solo porque `Player.sets` es una `List<Int>`.
cl.matchpoint.marcador.wear.domain.*

// Las listas de este proyecto nunca se mutan: se copian con `setAt`.
kotlin.collections.List
